<UserControl x:Class="ProgettoInformatica.Views.ShopWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:viewmodels="clr-namespace:ProgettoInformatica.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ShopWindowViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button Content="Madonna Lercia" Command="{Binding NavigationMenuCommand}"/>
        <CheckBox Content="{Binding CheckBoxName}" HorizontalAlignment="Left" Margin="525,157,0,0" VerticalAlignment="Top"/>


    </Grid>
</UserControl>
