<UserControl x:Class="ProgettoInformatica.Views.ShopWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:viewmodels="clr-namespace:ProgettoInformatica.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ShopWindowViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush x:Name="Gradient" StartPoint="1,1" EndPoint="0,0">
                <GradientStop x:Name="GradientStop1" Color="#3194F4" Offset="0.1"/>
                <GradientStop x:Name="GradientStop2" Color="#5571EC" Offset="0.7"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.Resources>
            <Style TargetType="{x:Type Border}">
                <Setter Property="CornerRadius" Value="5"/>
                <Setter Property="Background" Value="White"/>
            </Style>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="#5D70D1"/>
                <Setter Property="FontFamily" Value="Roboto"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
            </Style>
        </Grid.Resources>
        <!--<Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard >
                        <DoubleAnimationUsingKeyFrames BeginTime="0:0:0" Duration="0:0:6" Storyboard.TargetName="Gradient" RepeatBehavior="Forever" AutoReverse="True">
                            <LinearDoubleKeyFrame KeyTime="0:0:3" Storyboard.TargetProperty="(Grid.Background).(LinearGradientBrush.GradientStop)[0].(GradientStop.Offset)" Value="0.7">
                                
                            </LinearDoubleKeyFrame>
                            <LinearDoubleKeyFrame KeyTime="0:0:6" Storyboard.TargetName="GradientStop2" Value="0.1"></LinearDoubleKeyFrame>
                             
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>-->
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!--<Canvas Grid.ColumnSpan="7">
            <Image Source="./Img/options-icon.png" Height="40" Width="40" Canvas.Left="1225" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        </Canvas>-->
        <Grid Grid.ColumnSpan="7">
            <Grid.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="5"/>
                    <Setter Property="Background" Value="White"/>

                </Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Foreground" Value="#5D70D1"/>
                    <Setter Property="FontFamily" Value="Roboto"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                </Style>
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="600"/>
                <ColumnDefinition Width="340"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">

                                        </ContentPresenter>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="ForceCursor" Value="True"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Resources>
                <Button Background="Transparent" Command="{Binding NavigateMenuCommand}"  BorderThickness="0">

                    <Image Source="./Img/left-arrow.png" Margin="0 5"/>
                </Button>
                <TextBlock Text="Shop" VerticalAlignment="Center" FontSize="25" Foreground="White" FontFamily="Dilo World"/>
            </StackPanel>
            <Border Width="100" Height="20" HorizontalAlignment="Center" Margin="5 0 0 0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="level" Text="1" Padding="5 0">

                    </TextBlock>
                    <Border CornerRadius="0 6 6 0" Width="84" Background="LightGray">
                        <TextBlock x:Name="experience" Text="1/100"  Width="36"/>
                    </Border>
                </StackPanel>
            </Border>


            <Border Grid.Column="2" Width="80" Height="25" HorizontalAlignment="Center">
                <TextBlock  x:Name="coins" Text="1000" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image/>
                </TextBlock>
            </Border>

            <Image Grid.Column="2" HorizontalAlignment="Right" Source=".\Img\options-icon-white.png" Margin="0 0 15 0" Height="40"/>
        </Grid>
        <!--<StackPanel Orientation="Horizontal">
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">

                                    </ContentPresenter>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="ForceCursor" Value="True"/>
                            <Setter Property="Cursor" Value="Hand"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Resources>
            <Button Background="Transparent" Command="{Binding NavigationMenuCommand}"  BorderThickness="0">
                
                <Image Source="./Img/left-arrow.png" Margin="0 5"/>
            </Button>
            <TextBlock Text="Shop" VerticalAlignment="Center" FontSize="25" Foreground="White" FontFamily="Dilo World"/>
        </StackPanel>
            <Border Grid.Column="1" Width="100" Height="20" HorizontalAlignment="Left" Margin="5 0 0 0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="level" Text="1" Padding="5 0">

                    </TextBlock>
                    <Border CornerRadius="0 6 6 0" Width="84" Background="LightGray">
                        <TextBlock x:Name="experience" Text="1/100"  Width="35"/>
                    </Border>
                </StackPanel>
            </Border>


            <Border Grid.Column="5" Width="80" Height="25" HorizontalAlignment="Center">
                <TextBlock  x:Name="coins" Text="1000" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image/>
                </TextBlock>
            </Border>
            <Image Grid.Column="7" HorizontalAlignment="Right" Source=".\Img\options-icon-white.png"  Margin="0 0 10 0" Height="40"/>
        <TextBlock x:Name="TestoMain" Text="Deck Shop" Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="3" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="60" />-->

        <Grid Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="5" Grid.RowSpan="3"  ClipToBounds="False">
            <Grid.Resources>
                
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="FontFamily" Value="Roboto"/>
                    <Setter Property="Foreground" Value="#5D70D1"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">

                                    </ContentPresenter>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="0 0 10 10"/>
                        </Style>
                    </Style.Resources>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="ForceCursor" Value="True"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="BorderThickness" Value="10"/>
                            <Setter Property="BorderBrush" Value="#5D70D1"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="Margin" Value="60 0"/>
                    <Setter Property="Grid.RowSpan" Value="4"/>
                    <Setter Property="Background" Value="white"/> 
                    <Setter Property="CornerRadius" Value="15"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect
                                  ShadowDepth="10"
                                  Direction="300"
                                  Color="Gray"
                                  Opacity="0.5"
                                 BlurRadius="10"/>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="300"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>


            <Border Grid.Row="1" Grid.RowSpan="1" SnapsToDevicePixels="True">
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="215"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Nome Mazzo" />
                    <Image Grid.Row="1"/>
                    <Button Grid.Row="2" Content="Compra!"/>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="1" Grid.RowSpan="1" SnapsToDevicePixels="True">
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="215"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Nome Mazzo"/>
                    <Image Grid.Row="1"/>
                    <Button Grid.Row="2" Content="Compra!" Command=""/>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="2" Grid.RowSpan="1" SnapsToDevicePixels="True">
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="215"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Nome Mazzo"/>
                    <Image Grid.Row="1"/>
                    <Button Grid.Row="2" Content="Compra!" Command=""/>
                </Grid>
            </Border>

        </Grid>






    </Grid>
</UserControl>
